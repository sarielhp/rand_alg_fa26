%-----------------------------------------------------------------------------
% Master Prefix Router for Randomized Algorithms Class Notes & Book
%
% Dispatches to:
%   - prefix_lwarp.tex when warping/HTML mode is active (lwarpmk html or \warpMode)
%   - prefix_latex.tex for standard LaTeX / XeLaTeX / LuaLaTeX PDF builds
%-----------------------------------------------------------------------------

\makeatletter
\def\LWR@isWarp{0}%
\ifx\warpMode\undefined\else\def\LWR@isWarp{1}\fi
\expandafter\ifx\csname opt@lwarp.sty\endcsname\relax\else\def\LWR@isWarp{1}\fi

\if1\LWR@isWarp
  \IfFileExists{styles/prefix_lwarp.tex}{\input{styles/prefix_lwarp}}{\input{prefix_lwarp}}%
\else
  \IfFileExists{styles/prefix_latex.tex}{\input{styles/prefix_latex}}{\input{prefix_latex}}%
\fi
\makeatother
