\begin {remark} \remlab {epsilon:net:s}The above two theorems also hold for spaces with shattering dimension at most $\Dim $, in which case the sample size is slightly larger. Specifically, for \thmref {epsilon:net}, the sample size needed is $\ds O\pth { \frac {1}{\eps } \lg \frac {1}{\BadProb } + \frac {\Dim }{\eps }\lg \frac {\Dim }{\eps } }$. \end {remark}
